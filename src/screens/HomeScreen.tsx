import { Placeholder } from '@/components/Placeholder';

/**
 * Home — aktivitás-feed.
 *
 * Sávok fentről: hitelesítés-banner · köszöntő sor (terület-chip, GP-chip,
 * időjárás) · összegző kártya · NAPI KÜLDETÉS-KÁRTYA · Követett/Lokális
 * feed-váltó · feed.
 */
export function HomeScreen() {
  return (
    <Placeholder title="GRUNDO" spec="docs/02-funkcionalis-spec.md → Home">
      Itt lesz a feed, a napi küldetés és a saját összegződ.
    </Placeholder>
  );
}
